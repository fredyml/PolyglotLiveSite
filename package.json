{
  "name": "polyglot-live-marketing-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npm run build && node serve.mjs"
  }
}
